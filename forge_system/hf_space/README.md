# Jarvis SCI HF Space

Set `SCI_API_BASE_URL` to the deployed `forge_system` backend and `SCI_API_KEY` if the backend is protected.

The Space calls:
- `POST /api/jarvis/objectives`

It renders:
- objective graph summary
- model evaluation summary
- approval queue snapshot
